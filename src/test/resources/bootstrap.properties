# disable spring cloud in tests
aws.secretsmanager.enabled=false

# disable config, otherwise the CI server will try to fetch config from http://localhost:8888/application/ci
spring.cloud.config.enabled=false
