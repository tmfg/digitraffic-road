name: "Mirror repo to public"
on:
  push:
    branches:
      - master
jobs:
  Mirror-action:
    if: github.repository != 'tmfg/digitraffic-road'
    runs-on: ubuntu-26.04
    steps:
      - name: Mirror
        uses: tmfg/digitraffic-actions@c8414c3b4233ad587eee5b6e9c844cd6821cf22c # mirror/v2
        with:
          mirror-repo: git@github.com:tmfg/digitraffic-road.git
          ssh-private-key: ${{ secrets.SSH_MIRROR_KEY }}
